public void designUI() {
    // Setting button color to green
    button.setBackground(Color.GREEN);
    // Setting font size for better visibility
    button.setFont(new Font("Arial", Font.PLAIN, 16));
    
    panel.setLayout(new GridLayout(2, 2));  // Changing to grid layout for 2x2 button arrangement

    // Adding a new label component
    panel.setLayout(new GridLayout(2, 2));  // Changing to grid layout for 2x2 button arrangement

    // Adding a new label component (from Member 2)
    JLabel label = new JLabel("Enter your input:");
    panel.add(label);
    
}
